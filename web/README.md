# SemtSkoru — Frontend

Next.js (App Router) + TypeScript uygulaması. Kurulum, komutlar ve mimari için
kök dizindeki [`../README.md`](../README.md) esas kaynaktır.

Hızlı başlangıç (backend zaten `localhost:5169`'da çalışıyorsa):

```bash
npm install
npm run dev
# http://localhost:3000
```
