# Bu dosyayı .env olarak kopyalayın ve kendi şifrenizi belirleyin (.env git'e eklenmez).
# docker-compose.yml burayı okur; backend'in ConnectionStrings:Default'unda
# (dotnet user-secrets ile ayarlanır, bkz. README) aynı şifre kullanılmalı.
POSTGRES_PASSWORD=change_me_local_dev_password
